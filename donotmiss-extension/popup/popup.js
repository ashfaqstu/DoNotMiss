// Popup script - minimal for now
// Future: Will display AI-detected tasks from automated scraping

document.addEventListener('DOMContentLoaded', () => {
  // Popup is ready - currently just shows "coming soon" message
  console.log('DoNotMiss popup loaded');
  
  // Future: Load and display automatically detected tasks
  // For now, tasks are captured via right-click context menu
});
